[fusion]
logger = { level = "full", stdout = true }

[cubecl.profiling]
logger = { file = "/tmp/profiling.log", level = "disabled" }

[cubecl.autotune]
logger = { file = "/tmp/autotune.log", level = "disabled" }

[cubecl.compilation]
logger = { file = "/tmp/compilation.log", level = "disabled" }
