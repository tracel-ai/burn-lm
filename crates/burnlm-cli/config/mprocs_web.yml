procs:
  open-webui:
    shell: 'docker "logs" "-f" "open-webui"'
    autostart: true
    autorestart: true
    stop: SIGTERM
  openai-server:
    shell: 'cargo burnlm server run --port 3001'
    autostart: true
    autorestart: true
    stop: {send-keys: ["<C-c>"]}
