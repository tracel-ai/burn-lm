[profiling]
logger = { file = "/tmp/profiling.log", level = "disabled" }

[autotune]
logger = { file = "/tmp/autotune.log", level = "minimal" }

[compilation]
logger = { file = "/tmp/compilation.log", level = "full" }
cache = "target"
